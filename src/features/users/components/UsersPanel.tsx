"use client";
import { Copy, UserPlus } from "lucide-react";
import { useState } from "react";
import type { AppUser } from "@/features/auth/types/authTypes";
export function UsersPanel({ onCreateInvite, onRoleChange, users }: { onCreateInvite: (role: AppUser["role"]) => Promise<string>; onRoleChange: (id: string, role: AppUser["role"]) => void; users: AppUser[] }) { const [inviteRole, setInviteRole] = useState<AppUser["role"]>("Financeiro"); const [inviteCode, setInviteCode] = useState(""); return <div className="rounded-lg border border-slate-200 bg-white shadow-sm"><div className="border-b border-slate-200 px-5 py-4"><h3 className="font-semibold">Usuários e permissões</h3><p className="mt-1 text-sm text-slate-500">Dono administra; Financeiro altera dados; Consulta apenas visualiza.</p><div className="mt-4 flex flex-wrap gap-2"><select className="h-10 rounded-md border border-slate-300 bg-white px-3 text-sm" onChange={(event) => setInviteRole(event.target.value as AppUser["role"])} value={inviteRole}><option>Financeiro</option><option>Consulta</option><option>Dono</option></select><button className="inline-flex h-10 items-center gap-2 rounded-md bg-slate-950 px-4 text-sm font-semibold text-white" onClick={async () => setInviteCode(await onCreateInvite(inviteRole))} type="button"><UserPlus size={16} />Gerar convite</button>{inviteCode ? <button className="inline-flex h-10 items-center gap-2 rounded-md border border-slate-300 px-3 text-sm font-semibold" onClick={() => navigator.clipboard.writeText(inviteCode)} type="button"><Copy size={15} />{inviteCode}</button> : null}</div></div><div className="divide-y divide-slate-100">{users.map((item) => <div className="flex flex-col justify-between gap-3 px-5 py-4 sm:flex-row sm:items-center" key={item.id}><div><strong>{item.name}</strong><p className="text-sm text-slate-500">{item.email}</p></div><select className="h-10 rounded-md border border-slate-300 bg-white px-3 text-sm" onChange={(event) => onRoleChange(item.id, event.target.value as AppUser["role"])} value={item.role}><option>Dono</option><option>Financeiro</option><option>Consulta</option></select></div>)}</div></div>; }
