import { UserRound } from "lucide-react";
import type { AppUser } from "@/features/auth/types/authTypes";

export function UserProfile({ user }: { user: AppUser }) {
  return <div className="grid gap-5 lg:grid-cols-[280px_1fr]"><div className="rounded-lg border border-slate-200 bg-white p-6 text-center shadow-sm"><div className="mx-auto flex size-20 items-center justify-center rounded-full bg-cyan-50 text-cyan-700"><UserRound size={36} /></div><h3 className="mt-4 text-lg font-semibold">{user.name}</h3><p className="mt-1 text-sm text-slate-500">{user.email}</p><span className="mt-4 inline-block rounded-full bg-amber-50 px-3 py-1 text-xs font-semibold text-amber-800">{user.role}</span></div><div className="rounded-lg border border-slate-200 bg-white p-6 shadow-sm"><h3 className="font-semibold">Informações de acesso</h3><dl className="mt-5 grid gap-4 sm:grid-cols-2"><div><dt className="text-xs uppercase text-slate-500">Nome</dt><dd className="mt-1 font-medium">{user.name}</dd></div><div><dt className="text-xs uppercase text-slate-500">E-mail</dt><dd className="mt-1 font-medium">{user.email}</dd></div><div><dt className="text-xs uppercase text-slate-500">Perfil</dt><dd className="mt-1 font-medium">{user.role}</dd></div><div><dt className="text-xs uppercase text-slate-500">Tenant</dt><dd className="mt-1 font-medium">{user.companyName}</dd></div></dl></div></div>;
}
