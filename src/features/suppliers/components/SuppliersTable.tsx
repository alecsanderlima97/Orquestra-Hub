import { Pencil, Trash2 } from "lucide-react";
import type { Supplier } from "../types/supplierTypes";

export function SuppliersTable({ onDelete, onEdit, suppliers }: { onDelete?: (supplier: Supplier) => void; onEdit?: (supplier: Supplier) => void; suppliers: Supplier[] }) {
  return <div className="overflow-x-auto rounded-lg border border-slate-200 bg-white shadow-sm"><table className="w-full min-w-[900px] text-left text-sm"><thead className="bg-slate-50 text-slate-600"><tr><th className="px-5 py-3 font-medium">Fornecedor</th><th className="px-5 py-3 font-medium">CNPJ</th><th className="px-5 py-3 font-medium">Contato</th><th className="px-5 py-3 font-medium">Em aberto</th><th className="px-5 py-3 font-medium">Pagamento</th><th className="px-5 py-3 font-medium">Status</th><th className="px-5 py-3 font-medium">Ações</th></tr></thead><tbody className="divide-y divide-slate-100">{suppliers.length ? suppliers.map((supplier) => <tr key={supplier.id}><td className="px-5 py-4 font-medium text-slate-950"><div>{supplier.name}</div>{supplier.email ? <div className="text-xs font-normal text-slate-500">{supplier.email}</div> : null}</td><td className="px-5 py-4 text-slate-700">{supplier.document}</td><td className="px-5 py-4 text-slate-700"><div>{supplier.contactName || supplier.phone}</div>{supplier.contactName ? <div className="text-xs text-slate-500">{supplier.phone}</div> : null}</td><td className="px-5 py-4 font-medium text-slate-950">{supplier.openAmount}</td><td className="px-5 py-4 text-slate-700">{supplier.paymentMethod || "Não informado"}</td><td className="px-5 py-4"><span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-semibold text-slate-700">{supplier.status}</span></td><td className="px-5 py-4"><div className="flex gap-2"><button aria-label="Editar fornecedor" className="rounded-md border border-slate-200 p-2 text-slate-600 hover:bg-amber-50 hover:text-amber-800" onClick={() => onEdit?.(supplier)} title="Editar cadastro deste fornecedor" type="button"><Pencil size={17} /></button><button aria-label="Excluir fornecedor" className="rounded-md border border-slate-200 p-2 text-rose-600 hover:bg-rose-50" onClick={() => onDelete?.(supplier)} title="Excluir fornecedor sem movimentações vinculadas" type="button"><Trash2 size={17} /></button></div></td></tr>) : <tr><td className="px-5 py-8 text-center text-slate-500" colSpan={7}>Nenhum fornecedor encontrado.</td></tr>}</tbody></table></div>;
}
