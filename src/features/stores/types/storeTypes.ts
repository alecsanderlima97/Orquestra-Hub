export type Store = {
  id: string;
  name: string;
  manager: string;
  monthlyGoal: string;
  balance: string;
  cep?: string;
  address?: string;
  city?: string;
  state?: string;
  phone?: string;
  mapsUrl?: string;
  photoUrl?: string;
};
